Instruction subsystem (recipe shelf). The entry point AGENTS.md lives at the REPO ROOT
(agent convention), not here. This dir holds supporting/distributed instruction docs as
the repo grows. Fixes failure layer 2 (architecture knowledge). See L3/L4.
